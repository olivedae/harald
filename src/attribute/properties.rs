enum CharacteristicProperties {
    pub Broadcast,
    pub Read,
    pub WriteWithoutResponse,
    pub Write,
    pub Notify,
    pub Indicate,
    pub AuthenticatedSignedWrites,
    pub ExtendedProperties,
    pub NotifyEncryptionRequired,
    pub IndicateEncryptionRequired,
}
