use att::response::*;
use uuid::*;
use att::attribute::*;

struct AttServer;

impl AttServer {

}

#[cfg(test)]
mod tests {
    #[test]
    fn test_parse() {
        assert_eq!("rust", "rust");
    }
}
